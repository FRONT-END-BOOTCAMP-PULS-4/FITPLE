const ProjectDetailPage = ({ params: { id } }: { params: { id: string } }) => {
    return (
        <div>
            <div>방문페이지 : {id}</div>
        </div>
    );
};

export default ProjectDetailPage;
